# =========================
# BILYABITS-RAPI Environment
# =========================

# Server
PORT=5000
NODE_ENV=production
BASE_URL=https://your-domain.com

# Database Type: postgresql | supabase
# (mongodb support coming soon)
DB_TYPE=postgresql

# ─────────────────────────────────────
# PostgreSQL (when DB_TYPE=postgresql)
# ─────────────────────────────────────
DATABASE_URL=postgresql://user:password@host:5432/dbname
# DB_SSL=true  # Enable for cloud-hosted PostgreSQL (Render, Neon, etc.)

# ─────────────────────────────────────
# Supabase (when DB_TYPE=supabase)
# ─────────────────────────────────────
# Use your Supabase PostgreSQL connection string:
#   → Find it in: Supabase Dashboard > Project Settings > Database > Connection string (URI)
#   → Set it as DATABASE_URL below
# DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# ─────────────────────────────────────
# MongoDB (when DB_TYPE=mongodb) — coming soon
# ─────────────────────────────────────
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/dbname

# Session Secret (generate a random string)
SESSION_SECRET=your-random-secret-key-here

# Admin Credentials
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password
